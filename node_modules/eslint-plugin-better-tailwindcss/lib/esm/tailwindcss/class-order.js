import { resolve } from "node:path";
import { createSyncFn } from "synckit";
import { getTailwindConfigPath } from "./config.js";
import { getTailwindcssVersion } from "../async-utils/version.js";
import { getWorkerOptions } from "../async-utils/worker.js";
export function getClassOrder({ classes, configPath, cwd }) {
    const { path, warning } = getTailwindConfigPath({ configPath, cwd });
    const classOrder = getClassOrderSync({ classes, configPath: path });
    return { classOrder, warnings: [warning] };
}
const getClassOrderSync = createSyncFn(getWorkerPath(), getWorkerOptions());
function getWorkerPath() {
    const { major } = getTailwindcssVersion();
    return resolve(getCurrentDirectory(), `./class-order.async.v${major}.js`);
}
function getCurrentDirectory() {
    // eslint-disable-next-line eslint-plugin-typescript/prefer-ts-expect-error
    // @ts-ignore - `import.meta` doesn't exist in CommonJS -> will be transformed in build step
    return import.meta.dirname;
}
//# sourceMappingURL=class-order.js.map